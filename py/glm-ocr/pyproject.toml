[project]
name = "glm-ocr"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "ml-dtypes",
    "onnxruntime-gpu",
    "pillow",
    "transformers>=4.52",
]

[tool.setuptools.packages.find]
include = ["common*"]
