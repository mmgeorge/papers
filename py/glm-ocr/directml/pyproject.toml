[project]
name = "glm-ocr-directml"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "glm-ocr",
    "onnxruntime-directml>=1.24.2",
    "onnx",
    "pillow",
    "numpy",
]

[tool.uv.sources]
glm-ocr = { path = "..", editable = true }
